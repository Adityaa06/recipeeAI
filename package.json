{
    "name": "recipe-meal-planner",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "npm install --prefix server",
        "server-install": "npm install --prefix server",
        "build": "npm run server-install",
        "start": "node server/server.js"
    }
}